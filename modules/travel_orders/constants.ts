// Plain module constants (not a "use server" file, so non-async exports are fine).
// SLO uredba mileage rate. Constant for now; a later slice can make it editable.
export const RATE_EUR_PER_KM = 0.43;
