// Per-module config. Mirrors the registry entry; modules read their own config,
// never another module's.
export const config = {
  id: "work_orders",
  name: "Delovni nalogi",
  description:
    "Pisarna ustvari in dodeli delovni nalog (stranka iz Zoho); teren ga na telefonu premika skozi statuse do zaključka.",
};
