# /build — work_orders — 2026-06-29 20:38

- **Skill:** /build
- **Slice:** Slice 1 — modul `work_orders`: kreiranje/dodeljevanje/status + Zoho branje strank, mobile-first.
- **Commit:** `004a7f1`

## What was built
- `modules/_registry.ts` — registriran modul `work_orders` (ikona 🛠️).
- `modules/work_orders/module.config.ts` — config.
- `modules/work_orders/lib/zoho.ts` + `lib/mock-data.ts` — Zoho REST branje strank
  (Accounts), read-only, **mock fallback** brez poverilnic (po vzoru `intrix.ts`).
- `modules/work_orders/db/0001_work_orders.sql` — prefiksana tabela
  `work_orders_orders` s **prilagojenim RLS (ustvarjalec ALI dodeljeni)** +
  registracija/podelitev modula.
- `modules/work_orders/actions.ts` — `createWorkOrder`, `setWorkOrderStatus`
  (zod validacija, `userCanAccess` re-check, ime stranke razrešeno iz Zoho).
- `modules/work_orders/pages.tsx` — mobile-first: kreirni obrazec (stranka iz
  Zoho, izvajalec s spustnega seznama `listUsers`, naslov, opis), seznam kot
  kartice, detajl z gumbi za status (odprt → v_delu → zakljucen).
- `app/(app)/m/[module]/page.tsx` — dodan `case "work_orders"` (guard nedotaknjen).

## Checks
- `npm run lint` → ✓ brez napak.
- `npm run build` → ✓ uspešno (route `/m/[module]` zgrajen).

## Blocked / follow-up
- **Migracija ŠE NI uporabljena.** `npm run db:run` je vrnil **HTTP 401 (JWT could
  not be decoded)** — token Supabase Management API v `.env.local` je neveljaven/
  potekel. Dokler migracija ne steče, tabela ne obstaja in modul ne shranjuje/ne
  prikazuje. Reši: osveži `SUPABASE_ACCESS_TOKEN` in ponovno zaženi `npm run db:run --
  modules/work_orders/db/0001_work_orders.sql`, ali prilepi SQL v Supabase → SQL Editor.
- Po migraciji: lastnik si v **Admin** (`/m/admin`) podeli modul (+ izvajalcem).
