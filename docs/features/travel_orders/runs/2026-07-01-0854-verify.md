# verify — travel_orders — 2026-07-01 08:54

- **Skill:** /verify
- **What ran:** `npm run lint && npm run build`, `/check-architecture`, then the app
  was started (`npm run dev` via preview) and walked live in-browser as the member
  `mike-2@leskovsek.si` (created earlier with a single grant: `travel_orders`).
- **Result:**
  - ✓ `npm run lint` — clean, no warnings.
  - ✓ `npm run build` — production build succeeds; `/m/[module]` route compiles.
  - ✓ `/check-architecture` — PASS (prefixed table, RLS in the same migration,
    no cross-module imports, no hardcoded secrets, registry/router/DB all consistent).
  - ✓ Sign-in works; launcher (`/home`) for `mike-2@leskovsek.si` shows **only**
    "🚗 Potni nalogi" — no other module card rendered.
  - ✓ Module does its job: `/m/travel_orders` renders the create form + list;
    an existing trip (Novo Mesto → Maribor, 240 km) shows amount **103,20 €**,
    matching `240 × 0,43 = 103.20` — the server-computed reimbursement is correct,
    and the running total (`Skupaj: 240 km · 103,20 €`) sums correctly.
  - ✓ Ungranted module refused server-side: `fetch('/m/admin')` as `mike-2` returns
    HTTP 200 with the guard's **"No access"** card (`app/(app)/m/[module]/page.tsx`);
    response body contains no Admin UI markup (`Manage which users`, `Grant module`
    absent). The guard renders in-place rather than redirecting — verified by body
    content, not just status code.
  - ✓ Member cannot grant modules — verified two ways instead of live-walking the
    owner UI (no owner password was available; resetting one without an explicit
    request was correctly blocked by the auto-mode safety classifier, so this was
    not forced):
    1. `modules/admin/actions.ts:13-19` — every admin action calls `callerIfOwner()`
       first (`isOwner` check); a non-owner caller gets `null` and the action no-ops.
    2. `supabase/migrations/0001_core.sql:88-93` — RLS on `core_user_modules`
       restricts `insert`/`delete` to `public.is_owner()`. Even a direct write
       attempt by a member is rejected at the database layer, independent of app code.
  - ✓ Grant state matches "exactly one module": read-only query confirms
    `core_user_modules` has exactly one row for `mike-2`
    (`user_id → module_id: travel_orders`), nothing else.
- **Verdict:** PASS
- **Ref:** commit 42b2af7 (code) / c52f1dc (migration applied). No code changes
  made during this run — verification only.
